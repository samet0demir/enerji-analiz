{
  "name": "energy-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && npm run build",
    "start": "npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
